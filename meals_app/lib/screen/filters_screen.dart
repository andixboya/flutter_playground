import 'package:flutter/material.dart';

class FiltersScreen extends StatelessWidget {
  static const routeName = './filter';

  @override
  Widget build(BuildContext context) {
    return Text("Filters screen.");
  }
}
